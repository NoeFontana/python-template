# Architecture overview

<!-- TODO: Why the project is shaped the way it is — the `src/` layout, where
the package boundary sits, what belongs in `core` versus `cli`, and how the
public API is decided. Understanding-oriented: no steps, no API listings. Link
to the relevant records in `docs/adr/` rather than restating their reasoning.
-->
