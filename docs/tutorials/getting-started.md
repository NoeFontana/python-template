# Getting started

<!-- TODO: A ten-minute learning path from installation to a first working
result: install the package, call `greet()`, run one `Calculator` operation,
and invoke the CLI once. Learning-oriented — a guaranteed-to-succeed sequence,
not a menu of options. Every fenced `python` block after the first uses the
`continuation` fence option so state carries across the page. -->
