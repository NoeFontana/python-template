# How-to guides

<!-- TODO: An index of task-oriented recipes, one page per task, each answering
a "how do I ...?" a user arrives with. Task-oriented — assumes the reader
already knows what they want and needs the steps. Fenced `python` blocks here
run in isolation; each block must stand alone without `continuation`. -->
